Frontend (React + Vite)

To run the frontend during development:

```bash
cd frontend
npm install
npm run dev
```

The Vite server proxies `/api` and `/search_attendee` to the Flask backend running on `http://127.0.0.1:5000`.
